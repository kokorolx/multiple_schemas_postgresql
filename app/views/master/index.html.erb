<h1>Master#index</h1>
<p>Find me in app/views/master/index.html.erb</p>

<ul>
  <% @tenants.each do |tenant| %>
  <li> <%= link_to tenant.subdomain, "http://#{tenant.subdomain}.#{request.host}:#{request.port}/admin"  %></li>
  <% end -%>
</ul>
